/*========================================
	FlexFramewokr - UI Scripts
  ========================================*/
// TODO: Refactor jQuery scripts to vanilla JS



$(document).ready(function() {
	
	/****************************
	 * Affix
	 * 
	 * Fixes DOM element when user scrolls (e.g. fixed menu bar)
	 ****************************/
	// for `data-watcher` attributes set to `affix`
	// also need to set `data-offset` (to specify top/right position)
	// e.g. `<nav class='nav' data-watcher='affix' data-offset-top='200'>`

	// TODO: need to do for every element set to `affix` (i.e. loop)
	// .addClass('affix');
	
	
	/****************************
	 * Collapse/ Collapse group
	 *
	 * Hide and show elements via 'blinds'
	 ****************************/
	
	
	/****************************
	 * Dropdown
	 *
	 * creates drop-down menus after after clicking
	 ****************************/
	
	/***************************
	 * Carosel
	 *
	 * cycles through elements -- e.g. slideshow
	 ***************************/
	
});
